apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "hpachart.fullname" . }}-test-connection"
  labels:
    {{- include "hpachart.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "hpachart.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
