apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: ashu-pv-claim
spec:
  storageClassName: manual
  accessModes:
    - ReadWriteMany
  resources:
    requests:
      storage: 3Gi # size we need 